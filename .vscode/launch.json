{
  // 使用 IntelliSense 了解相关属性。
  // 悬停以查看现有属性的描述。
  // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LLDB: Build and debug active file (nvim test)",
      "type": "codelldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}.out",
      "args": [],
      // "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      // "environment": [],
      // "console": "integratedTerminal",
      // "externalConsole": false, // set true to enable keyboard input
      // "MIMode": "lldb",
      // "preLaunchTask": "gcc build c"
      // "preLaunchTask": "C/C++: clang build active file"
      "preLaunchTask": "C/C++: clang build active file (nvim test)"
    }
  ]
}
